input_string = input("Enter a string with digits: ")
sum_of_digits = 0
for char in input_string:
    if char.isdigit():  
        sum_of_digits += int(char)  
print(f"The sum of the digits in the string is: {sum_of_digits}")
